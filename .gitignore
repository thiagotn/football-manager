# Editors / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Claude Code
.claude/

# Database dumps — contain PII, never commit
dump-*.sql
*.dump

# Secrets — never commit these
.env
.env.docker
.env.local
.env.prod

# Configurações de infra local (obsoletas)
proxy/
