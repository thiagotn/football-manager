# Grafana Unified Alerting — provisionamento automático
# Documentação: https://grafana.com/docs/grafana/latest/alerting/set-up/provision-alerting-resources/file-provisioning/
#
# Pipeline de cada regra: Query (A) → Reduce (B) → Threshold (C)
#   A: busca a métrica no Prometheus
#   B: reduz a série temporal para um único valor (última leitura)
#   C: aplica a condição de disparo
#
# Para adicionar notificações (Telegram, e-mail, etc.):
#   Crie um contact point em Alerting → Contact points e configure
#   a notification policy em Alerting → Notification policies.
#
# Nota sobre a métrica de 5xx: prometheus-fastapi-instrumentator v7 expõe
# o histograma http_request_duration_seconds com label "status_code".
# Se após o deploy os dados não aparecerem, verifique o nome real em
# https://api.rachao.app/metrics e ajuste a linha `expr` abaixo.

apiVersion: 1

# Regras removidas do provisionamento precisam ser deletadas explicitamente,
# senão permanecem no banco do Grafana e continuam disparando.
# API v2 (Go) desativada no VPS em 2026-07-16 (migração para o homelab).
# API v1 (Python) descomissionada na VPS em 2026-07-31 (cutover completo
# para o homelab): removidas as regras de API v1 e do scheduler v1.
deleteRules:
  - orgId: 1
    uid: alert-5xx-rate-high-v2
  - orgId: 1
    uid: alert-api-v2-down
  - orgId: 1
    uid: alert-5xx-rate-high
  - orgId: 1
    uid: alert-api-v1-down
  - orgId: 1
    uid: alert-scheduler-mute
  - orgId: 1
    uid: alert-recurrence-stale
  - orgId: 1
    uid: alert-scheduler-failures

groups:
  # ──────────────────────────────────────────────────────────────────────
  # Infraestrutura: RAM, disco e CPU do host (via Node Exporter)
  # ──────────────────────────────────────────────────────────────────────
  - orgId: 1
    name: Infraestrutura
    folder: Alertas
    interval: 1m
    rules:

      # ── RAM > 85% por 5 minutos ──────────────────────────────────────
      - uid: alert-ram-high
        title: "RAM > 85%"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 300
              to: 0
            datasourceUid: prometheus
            model:
              editorMode: code
              expr: "(node_memory_MemTotal_bytes - node_memory_MemAvailable_bytes) / node_memory_MemTotal_bytes * 100"
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: B
            relativeTimeRange:
              from: 300
              to: 0
            datasourceUid: __expr__
            model:
              refId: B
              type: reduce
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              reducer: last
              settings:
                mode: dropNN
          - refId: C
            relativeTimeRange:
              from: 300
              to: 0
            datasourceUid: __expr__
            model:
              refId: C
              type: threshold
              datasource:
                type: __expr__
                uid: __expr__
              expression: B
              conditions:
                - evaluator:
                    params:
                      - 85
                    type: gt
                  operator:
                    type: and
                  query:
                    params:
                      - C
                  reducer:
                    params: []
                    type: last
                  type: query
        noDataState: Alerting
        execErrState: Alerting
        for: 5m
        isPaused: false
        labels:
          severity: warning
        annotations:
          summary: "RAM do servidor acima de 85%"
          description: "Uso atual: {{ with $values.B }}{{ if .Value }}{{ .Value | printf \"%.1f\" }}{{ else }}N/A{{ end }}{{ else }}N/A{{ end }}%"

      # ── Disco > 80% ──────────────────────────────────────────────────
      - uid: alert-disk-high
        title: "Disco > 80%"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 300
              to: 0
            datasourceUid: prometheus
            model:
              editorMode: code
              expr: "(node_filesystem_size_bytes{mountpoint=\"/\"} - node_filesystem_avail_bytes{mountpoint=\"/\"}) / node_filesystem_size_bytes{mountpoint=\"/\"} * 100"
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: B
            relativeTimeRange:
              from: 300
              to: 0
            datasourceUid: __expr__
            model:
              refId: B
              type: reduce
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              reducer: last
              settings:
                mode: dropNN
          - refId: C
            relativeTimeRange:
              from: 300
              to: 0
            datasourceUid: __expr__
            model:
              refId: C
              type: threshold
              datasource:
                type: __expr__
                uid: __expr__
              expression: B
              conditions:
                - evaluator:
                    params:
                      - 80
                    type: gt
                  operator:
                    type: and
                  query:
                    params:
                      - C
                  reducer:
                    params: []
                    type: last
                  type: query
        noDataState: Alerting
        execErrState: Alerting
        for: 5m
        isPaused: false
        labels:
          severity: warning
        annotations:
          summary: "Disco do servidor acima de 80%"
          description: "Uso atual: {{ with $values.B }}{{ if .Value }}{{ .Value | printf \"%.1f\" }}{{ else }}N/A{{ end }}{{ else }}N/A{{ end }}%"

      # ── CPU > 90% por 10 minutos ─────────────────────────────────────
      - uid: alert-cpu-high
        title: "CPU > 90%"
        condition: C
        data:
          - refId: A
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: prometheus
            model:
              editorMode: code
              expr: "100 - (avg(rate(node_cpu_seconds_total{mode=\"idle\"}[5m])) * 100)"
              instant: true
              intervalMs: 1000
              maxDataPoints: 43200
              refId: A
          - refId: B
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: __expr__
            model:
              refId: B
              type: reduce
              datasource:
                type: __expr__
                uid: __expr__
              expression: A
              reducer: last
              settings:
                mode: dropNN
          - refId: C
            relativeTimeRange:
              from: 600
              to: 0
            datasourceUid: __expr__
            model:
              refId: C
              type: threshold
              datasource:
                type: __expr__
                uid: __expr__
              expression: B
              conditions:
                - evaluator:
                    params:
                      - 90
                    type: gt
                  operator:
                    type: and
                  query:
                    params:
                      - C
                  reducer:
                    params: []
                    type: last
                  type: query
        noDataState: Alerting
        execErrState: Alerting
        for: 10m
        isPaused: false
        labels:
          severity: critical
        annotations:
          summary: "CPU do servidor acima de 90%"
          description: "Uso atual: {{ with $values.B }}{{ if .Value }}{{ .Value | printf \"%.1f\" }}{{ else }}N/A{{ end }}{{ else }}N/A{{ end }}%"

  # ──────────────────────────────────────────────────────────────────────
  # (grupos "API" e "Scheduler" removidos em 2026-07-31 — API v1
  #  descomissionada na VPS; ver deleteRules no topo deste arquivo)
  # ──────────────────────────────────────────────────────────────────────
