version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - gosec
    - misspell
    - exhaustive
    - noctx
    - bodyclose
    - ineffassign
    - unused
  settings:
    gosec:
      excludes:
        - G115
    exhaustive:
      default-signifies-exhaustive: true
  exclusions:
    rules:
      - path: "_test\\.go"
        linters:
          - gosec
          - errcheck

run:
  timeout: 5m
