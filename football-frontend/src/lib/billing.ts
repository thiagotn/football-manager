/**
 * Feature flag: habilita fluxo de billing (Stripe).
 *
 * Para habilitar localmente: mude para true.
 * Antes de fazer deploy em produção: mude para false.
 */
export const billingEnabled = true;
